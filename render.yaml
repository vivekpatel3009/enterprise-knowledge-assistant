# Render deployment configuration for Enterprise Knowledge Assistant

services:
  - type: web
    name: enterprise-knowledge-assistant
    env: python
    plan: starter
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn backend.src.main:app --host 0.0.0.0 --port 8080"
